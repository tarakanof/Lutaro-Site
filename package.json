{
  "name": "lutaro-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test src/lib/",
    "check-links": "node scripts/check-links.mjs",
    "check": "npm test && npm run build && npm run check-links"
  },
  "dependencies": {
    "astro": "^5.2.0"
  },
  "engines": {
    "node": ">=22"
  }
}
